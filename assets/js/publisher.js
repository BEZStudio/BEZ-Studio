import { BEZ } from './app.js';
function formObj(f){return Object.fromEntries(new FormData(f).entries())}
document.addEventListener('DOMContentLoaded',()=>{const f=document.querySelector('#metadataForm');if(!f)return;f.addEventListener('submit',e=>{e.preventDefault();const d=formObj(f);const obj={slug:d.slug.trim(),title:d.title.trim(),version:d.version.trim(),genre:d.genre,platform:'Windows',developer:'BEZ Studio',short_description:d.short_description.trim(),long_description:d.long_description.trim(),tags:d.tags.split(',').map(x=>x.trim()).filter(Boolean),cover_image_url:d.cover_image_url.trim(),release_date:d.release_date,file_size_human:d.file_size_human.trim(),download_url:d.download_url.trim(),storage_path:d.storage_path.trim()};document.querySelector('#metadataOutput').value=JSON.stringify(obj,null,2);BEZ.toast('Game metadata generated.','ok')});document.querySelector('#copyMetadata')?.addEventListener('click',async()=>{const o=document.querySelector('#metadataOutput');await navigator.clipboard.writeText(o.value);BEZ.toast('Copied JSON.','ok')})});
